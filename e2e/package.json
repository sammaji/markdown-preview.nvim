{
  "name": "markdown-preview-e2e",
  "private": true,
  "description": "End-to-end tests: a real Neovim drives the release server binary, a real browser shows the preview",
  "license": "MIT",
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.6.2",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20.9"
  }
}
